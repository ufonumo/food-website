import ImageThree from '../assets/imagethree.jpg';
import ImageFive from '../assets/imagefive.jpg';


export const InfoData = {
    heading: 'Eat our delicious and mouth-watering food',
    paragraphOne: 'When you use component, the router uses React.createElement to create a new React element from the given component.  That means if you provide an inline function to the component attribute, you would create a new component every render. This results in the existing component unmounting and the new component mounting instead of just updating the existing component.',
    paragraphTwo: 'When you use component, the router uses React.createElement This results in the existing component unmounting and the new component mounting instead of just updating the existing component.',
    buttonLabel: 'View Food',
    image: ImageFive,
    reverse: false,
    delay: 100
}

export const InfoDataTwo = {
    heading: 'Eat our delicious and mouth-watering food',
    paragraphOne: 'When you use component, the router uses React.createElement to create a new React element from the given component.  Tou would create a new component every render. This results in the existing component unmounting and the new component mounting instead of just updating the existing component.',
    paragraphTwo: 'When you use component, the router uses React.createElement This results in the existing co the new component mounting instead of just updating the existing component.',
    buttonLabel: 'View Food',
    image: ImageThree,
    reverse: false,
    delay: 300
}